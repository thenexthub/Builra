import sys

## Autogenerated by builra configuration.
# Do not edit!
config.builra_src_root = "@BUILRA_SRC_DIR@"
config.builra_obj_root = "@BUILRA_OBJ_DIR@"
config.builra_build_mode = "@BUILRA_BUILD_MODE@"

# Support substitution of the build_mode with user parameters. This is
# used when we can't determine the tool dir at configuration time.
try:
    config.builra_build_mode = config.builra_build_mode % lit_config.params
except KeyError:
    e = sys.exc_info()[1]
    key, = e.args
    lit_config.fatal("unable to find %r parameter, use '--param=%s=VALUE'" % (
        key, key))

# Let the main config do the real work.
lit_config.load_config(config, "@BUILRA_SRC_DIR@/tests/Unit/lit.cfg")
