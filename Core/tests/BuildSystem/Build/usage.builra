# Check basic usage.
#
# RUN: %{builra} buildsystem build --help 2> %t.err
# RUN: %{FileCheck} < %t.err %s

# CHECK: Usage: builra buildsystem build
# CHECK: --help
# CHECK: --chdir <PATH>
# CHECK: --trace <PATH>
